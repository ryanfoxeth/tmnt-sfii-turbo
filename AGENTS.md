# Agent entry point

Read `docs/handoff/README.md` and `docs/handoff/STATUS.md` first. v29 is the
current playable release; older versioned prose describes historical scope.
This repository is the public patch distribution, not the entire private
editing workspace. Source-workbench and cross-device restoration directions
are in the handoff. Do not publish ROMs, states, extracted original assets or
standalone source recordings. Follow CONTRIBUTING.md and the exact allowlist
in scripts/audit_public_tree.py. Add intentional new public files to that
allowlist; preserve all pinned existing patch hashes.

Use small scoped changes, synthetic tests and the public audit. Local ROM
checks stay outside the repo. Track current work honestly; turtle idle is open,
Music Lab paused, short shared hurt grunts and April KO intentionally stock.
For Ryan's repository work also update the StarDeck project note and actual
local-date daily log, resolving the vault path on the current host.
